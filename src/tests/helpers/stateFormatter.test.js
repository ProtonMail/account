import { extended } from '../../helpers/stateFormatter';
import { shallow } from 'preact-render-spy/index';

const state = {
    a: {
        c: {
            e: 1,
            g: 'arabica'
        }
    },
    b: {
        d: {
            f: 2
        },
        h: 256
    }
};

